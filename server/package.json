{
  "name": "bedrock-advisor-server",
  "version": "0.1.0",
  "private": true,
  "description": "Local stdio MCP server that exposes a consult tool backed by `claude -p --model opus`.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/*.test.js",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
